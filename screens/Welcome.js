import React from "react";
import { StyleSheet, Text, View, Button } from 'react-native';

const WelcomeScreen = ({navigation, route}) => {
    return <Text>This is Welcome Page</Text>;
  };

  export default WelcomeScreen;